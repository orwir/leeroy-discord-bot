import chai from 'chai'
import * as tested from '../../src/internal/storage.mjs'

const expect = chai.expect

describe('Storage Test Suite', () => {

    describe('.save(id, object)', () => {

        it('tests are created', () => {
            expect('TODO').to.equal('created tests')
        })

    })

    describe('.obtain(id, def)', () => {

        it('tests are created', () => {
            expect('TODO').to.equal('created tests')
        })

    })

    describe('.remove(id)', () => {

        it('tests are created', () => {
            expect('TODO').to.equal('created tests')
        })

    })

})